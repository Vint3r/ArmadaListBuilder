import { FighterDefenseTokenId } from './fighter-defense-token-id';

describe('FighterDefenseTokenId', () => {
  it('should create an instance', () => {
    expect(new FighterDefenseTokenId()).toBeTruthy();
  });
});
