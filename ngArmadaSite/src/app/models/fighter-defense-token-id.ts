export class FighterDefenseTokenId {

  // FIELDS

  fighterId: number;
  defenseId: number;

  // CONSTRUCTOR

  constructor(fighterId: number, defenseId: number) {
    this.fighterId = fighterId;
    this.defenseId = defenseId;
  }
}
