import { FighterDefenseToken } from './fighter-defense-token';

describe('FighterDefenseToken', () => {
  it('should create an instance', () => {
    expect(new FighterDefenseToken()).toBeTruthy();
  });
});
