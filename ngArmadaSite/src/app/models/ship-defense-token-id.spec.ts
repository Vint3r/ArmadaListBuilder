import { ShipDefenseTokenId } from './ship-defense-token-id';

describe('ShipDefenseTokenId', () => {
  it('should create an instance', () => {
    expect(new ShipDefenseTokenId()).toBeTruthy();
  });
});
