export class ListFighterId {

  // FIELDS

  fighterId: number;
  listId: number;

  // CONSTRUCTOR

  constructor(fighterId: number, listId: number) {
    this.fighterId = fighterId;
    this.listId = listId;
  }
}
