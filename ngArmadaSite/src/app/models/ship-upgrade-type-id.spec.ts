import { ShipUpgradeTypeId } from './ship-upgrade-type-id';

describe('ShipUpgradeTypeId', () => {
  it('should create an instance', () => {
    expect(new ShipUpgradeTypeId()).toBeTruthy();
  });
});
