export class ShipUpgradeTypeId {

  // FIELDS

  shipId: number;
  upgradeId: number;

  // CONSTRUCTOR

  constructor(shipId: number, upgradeId: number) {
    this.shipId = shipId;
    this.upgradeId = upgradeId;
  }
}
